<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" 
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans 
    http://www.springframework.org/schema/beans/spring-beans.xsd">

    <description>Shiro安全配置</description>
    <!--安全管理器-->
    <bean id="securityManager" class="org.apache.shiro.web.mgt.DefaultWebSecurityManager">
        <!--设置自定义Realm-->
        <property name="realm" ref="userRealm"/>
    </bean>
	<!-- 項目自定义的Realm -->
    <bean id="userRealm" class="com.hengtian.common.shiro.UserRealm"></bean>

    <!-- Shiro Filter -->
    <bean id="shiroFilter" class="org.apache.shiro.spring.web.ShiroFilterFactoryBean">
        <!-- 安全管理器 -->
        <property name="securityManager" ref="securityManager"/>
        <!-- 默认的登陆访问url -->
        <property name="loginUrl" value="/login"/>
        <!-- 登陆成功后跳转的url -->
        <property name="successUrl" value="/index"/>
        <!-- 没有权限跳转的url -->
        <property name="unauthorizedUrl" value="/login"/>
        <property name="filterChainDefinitions">
            <value>
                <!-- anon:不需要认证    authc:需要认证   user:验证通过或RememberMe登录的都可以 -->
                /resource/** = anon   		<!--系统静态文件  -->
                /login = anon		  		<!--登录入口  -->
                /dingapi/** = anon   		<!--钉钉的接口地址  -->
                /swagger-ui.* = anon  		<!--springfox-swagger-ui的静态文件  -->
                /webjars/** = anon		  	<!--springfox-swagger-ui的静态文件  -->
                /configuration/** = anon	<!--springfox-swagger-ui的静态文件  -->
                /v2/api-docs = anon			<!--springfox-swagger-ui的静态文件  -->
                /swagger-resources = anon	<!--springfox-swagger-ui的静态文件  -->
                /editor-app = anon	        <!--springfox-swagger-ui的静态文件  -->
                /diagram-viewer = anon	    <!--springfox-swagger-ui的静态文件  -->
                /sysUser/passwordForget = anon
                /sysUser/passwordReset = anon
                /sysUser/resetPassword = anon
                /createCaptcha = anon
                /sysUser/password/mailValidate = anon
                /rest/** = anon
                /diagram-viewer/** = anon
                /** = authc					<!--其余的全部需要认证  -->
            </value>
        </property>
    </bean>
    
    <!-- 保证实现了Shiro内部lifecycle函数的bean执行 -->  
	<bean id="lifecycleBeanPostProcessor" class="org.apache.shiro.spring.LifecycleBeanPostProcessor"/>  
    
    <!-- 开启Shiro注解 -->
	<bean class="org.springframework.aop.framework.autoproxy.DefaultAdvisorAutoProxyCreator" depends-on="lifecycleBeanPostProcessor"/>  
  	<bean class="org.apache.shiro.spring.security.interceptor.AuthorizationAttributeSourceAdvisor">  
  	  <property name="securityManager" ref="securityManager"/>  
    </bean>
    
</beans>